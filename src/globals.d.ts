// Injected at build time by vite.config.ts (`define`). The version string of
// this build — either the release tag (CI), `git describe` output, or the
// package.json version. Compared against the latest GitHub release in
// UpdateBanner.tsx.
declare const __APP_VERSION__: string
